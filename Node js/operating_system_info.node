const os = require("os");

console.log("Operating System Information:\n");

Array.from(Object.keys(os)).forEach(k=>{

    try{
    
        console.log(`${k}: ${JSON.stringify(os[k]())}\n`);
        
    }catch(err){}

});


